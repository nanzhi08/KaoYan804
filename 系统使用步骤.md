# 系统使用步骤

## 1. 启动系统

推荐一键启动：

```powershell
python start.py
```

手动启动后端：

```powershell
cd backend
python run.py
```

手动启动前端：

```powershell
cd frontend
npm run dev
```

访问地址：

- 前端：`http://localhost:5173`
- 后端健康检查：`http://localhost:8000/api/health`
- Swagger：`http://localhost:8000/docs`

## 2. 每日学习流程

1. 打开“学习仪表盘”。
2. 查看“今日刷题目标”。
3. 先点击“查看复习计划”，完成到期知识点回忆和评分。
4. 回到仪表盘或“学习统计”，查看薄弱知识点。
5. 点击“刷这个知识点”，进入定向刷题。
6. 完成一组题后查看解析。
7. 回到“学习统计”观察掌握度和薄弱队列变化。

## 3. 今日刷题目标说明

仪表盘目标来自后端 `/api/progress/overview`。

- 今日已完成：本地今日 0 点后的刷题记录数。
- 待复习知识点：未排程或已经到期的知识点数。
- 薄弱知识点：掌握度低于 40% 的知识点数。
- 今日目标：基础 10 题，加上到期复习和薄弱点加权，最高 30 题。

## 4. 刷题练习

进入“刷题练习”后可以：

- 选择题型，也可以选择“全部题型”。
- 选择科目范围。
- 设置题目数量和难度。
- 点击“开始刷题”随机练习。
- 点击“按章节浏览”按章节练习。
- 从仪表盘或学习统计进入时，系统会自动携带薄弱知识点 ID 做定向抽题。

提交答案后，系统会：

- 显示正确答案和解析。
- 写入练习记录。
- 更新题目关联知识点的掌握度。
- 安排下一次复习时间。

## 5. 复习计划

进入“复习计划”后：

1. 查看当前到期或尚未排程的知识点。
2. 先回忆该知识点。
3. 根据回忆质量评分。
4. 系统按 SM-2 规则更新下一次复习时间。

`next_review_at` 为空的知识点会被视为现在需要复习。

## 6. 学习统计

“学习统计”用于查看：

- 总刷题数和正确率。
- C 语言与数据结构分科正确率。
- 到期复习数量。
- 低掌握知识点数量。
- 优先补弱队列。
- 章节掌握度雷达图。

建议优先处理“优先补弱队列”的前几项。

## 7. 错题记录

进入“错题记录”后：

- 查看最近错题。
- 重新练习错题。
- 结合解析定位错因。
- 再回到刷题练习做同类知识点巩固。

## 8. 知识地图

进入“知识地图”后：

- 查看 C 语言和数据结构的知识点树。
- 点击知识点查看详情。
- 可以结合 AI 讲解理解概念。
- 学完后进入刷题或复习计划巩固。

## 9. AI 导师和历史回答

进入“AI 导师”可以提问。

进入“历史回答”可以查看过去的 AI 对话记录。

如果 AI 接口不可用，先检查：

- `DEEPSEEK_API_KEY` 是否配置。
- 后端是否正常运行。
- `/api/health` 是否返回 200。

## 10. 资料管理

进入“资料管理”后可以上传：

- PDF
- 图片
- Markdown
- Text
- Docx

上传后系统会保存文件并提取文本。删除资料前确认不再需要该文件。

## 11. 模拟考试

进入“模拟考试”后：

1. 选择试卷配置。
2. 生成试卷。
3. 开始作答。
4. 提交后查看结果。

如果只想日常巩固，优先使用“刷题练习”和“复习计划”。

## 12. 常见问题

### 前端出现 502

先检查后端：

```powershell
Invoke-WebRequest http://127.0.0.1:8000/api/health
```

再检查前端代理配置：

```text
frontend/vite.config.ts -> /api -> http://127.0.0.1:8000
```

### 今日刷题数不增长

确认答题提交成功，并刷新仪表盘。系统现在按本地今日 0 点换算到 UTC 查询，可以避免北京时间凌晨统计为 0。

### `/code-review` 不能用

当前原因是 Claude Code 找不到 Git Bash 路径：

```text
D:\Program Files\Git\bin\bash.exe
```

同时当前终端里 `git` 也不在 PATH。修复方式：

1. 安装 Git for Windows。
2. 确认存在 `git.exe` 和 `bash.exe`。
3. 修正 Claude Code 的 `CLAUDE_CODE_GIT_BASH_PATH`。
4. 修正 `.mcp.json` 中 `GIT_PYTHON_GIT_EXECUTABLE` 指向真实 `git.exe`。
5. 重新运行：

```powershell
claude mcp list
```

## 13. 推荐检查命令

后端测试：

```powershell
python -m unittest backend.tests.test_core_behaviors
```

前端检查：

```powershell
cd frontend
npm run lint
npm run build
```

浏览器使用时，以前端控制台没有 error/warning、Network 中 `/api/*` 无 400/500 为通过标准。
