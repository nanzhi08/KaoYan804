# 考研知识库系统项目详细说明书

## 1. 项目概述

本项目是一个面向上海第二工业大学 804《数据结构与高级程序设计》考研备考场景构建的专题学习系统。系统将知识点管理、章节学习、刷题训练、错题记录、AI 辅导、模拟考试、复习计划、学习统计、文档上传与 OCR 识别等能力整合到一个前后端分离的应用中。

从当前代码结构来看，它不是一个通用知识库产品，而是一个围绕 804 专业课复习闭环设计的垂直业务系统，目标是帮助考生完成以下学习链路：

- 看知识点
- 做章节题或随机题
- 记录答题结果与错题
- 使用 AI 进行知识讲解和答案批改
- 生成模拟试卷
- 根据掌握度安排复习
- 通过统计页面查看整体学习情况
- 上传 PDF/图片资料并自动提取文本

## 2. 项目定位与业务目标

### 2.1 业务定位

该项目定位为一个考研 804 专业课智能学习平台，覆盖以下两大考试内容：

- 高级程序设计
- 数据结构

项目内已有大量配套资料与脚本，说明它不仅是一个纯演示系统，也兼具本地资料管理、题库建设、考试训练和 AI 助教能力。

### 2.2 主要业务模块

- 仪表盘首页
- 知识地图
- 刷题练习
- AI 导师
- 模拟考试
- 复习计划
- 资料管理
- 学习统计

### 2.3 项目特点

- 面向单一考试科目，领域边界清晰
- 前后端分离，结构简洁，适合本地开发和后续扩展
- 后端按业务拆分接口，模块职责相对明确
- 已引入 AI 和 OCR，具备较强扩展潜力
- 使用 SQLite，便于本地快速启动与演示

## 3. 总体技术架构

### 3.1 技术栈总览

前端技术栈：

- React 19
- TypeScript
- Vite
- Ant Design
- React Router
- Zustand
- Axios
- Recharts
- React Markdown
- Monaco Editor

后端技术栈：

- FastAPI
- SQLAlchemy 2.0 Async
- SQLite
- Uvicorn
- Pydantic Settings

AI 与文档处理：

- DeepSeek Provider
- Claude Provider
- SSE 流式输出
- EasyOCR
- pdfplumber
- PyMuPDF
- Pillow

### 3.2 架构形态

项目采用典型的前后端分离结构：

1. 前端使用 React 构建单页应用，负责页面渲染、路由跳转、交互和数据展示。
2. 后端使用 FastAPI 提供 REST API 和流式 AI 接口。
3. 数据库存储在本地 SQLite 文件中。
4. 上传的文档保存在 `data/uploads/` 目录。
5. 前端开发环境通过 Vite 代理将 `/api` 请求转发至后端服务。

### 3.3 架构关系图

```text
┌─────────────────────┐
│   React + Vite 前端  │
│  页面/组件/状态/API  │
└─────────┬───────────┘
          │ /api
          ▼
┌─────────────────────┐
│    FastAPI 后端      │
│  路由/API/服务层     │
└──────┬──────┬───────┘
       │      │
       │      ├──────────────┐
       │                     │
       ▼                     ▼
┌──────────────┐   ┌──────────────────┐
│ SQLite 数据库 │   │ OCR / AI Provider │
│ knowledge.db │   │ DeepSeek/Claude   │
└──────────────┘   └──────────────────┘
       │
       ▼
┌──────────────────┐
│  data/uploads/   │
│ 上传文件与文档文本 │
└──────────────────┘
```

## 4. 项目目录结构说明

## 4.1 根目录结构

```text
考研知识库系统/
├── frontend/               前端工程
├── backend/                后端工程
├── data/                   数据库与上传文件
├── 二工大804资料包/         本地考试资料包
├── .claude/                本地技能与工具配置
├── CLAUDE.md               项目架构与使用说明
├── export_questions_doc.py 题库导出脚本
├── OCR识别效果展示报告.md   OCR 效果报告
└── 其他资料与文档文件
```

### 4.2 `frontend/` 前端目录说明

```text
frontend/
├── public/                 静态资源
├── src/
│   ├── components/         通用组件
│   ├── pages/              页面级模块
│   ├── services/           前端 API 封装
│   ├── stores/             Zustand 状态管理
│   ├── types/              TypeScript 类型定义
│   ├── App.tsx             路由总入口
│   ├── main.tsx            应用入口
│   └── index.css           全局样式
├── package.json            前端依赖与脚本
├── vite.config.ts          Vite 配置
└── tsconfig*.json          TypeScript 配置
```

各子目录职责如下：

- `components/AIChat/`：AI 聊天窗口、模型选择器等复用组件
- `components/Layout/`：系统主布局，负责左侧菜单和页面容器
- `pages/`：每个业务页面一个目录
- `services/`：对后端接口的封装，统一从此层发起请求
- `stores/`：前端状态管理，主要保存当前知识点和刷题流程状态
- `types/`：前后端交互所需的数据类型

### 4.3 `backend/` 后端目录说明

```text
backend/
├── app/
│   ├── api/                路由层
│   ├── ai_providers/       AI 供应商适配层
│   ├── models/             ORM 数据模型
│   ├── schemas/            Pydantic 数据结构
│   ├── services/           业务服务层
│   ├── config.py           项目配置
│   ├── database.py         数据库连接与会话
│   └── main.py             FastAPI 应用入口
├── seed/                   种子数据脚本
├── requirements.txt        后端依赖
├── .env                    环境变量文件
└── run.py                  启动入口
```

各子目录职责如下：

- `api/`：定义 HTTP 路由，将请求分发到服务层
- `ai_providers/`：封装不同大模型厂商的调用实现
- `models/`：定义数据库表结构与关系
- `schemas/`：定义接口请求与响应的数据格式
- `services/`：承载业务逻辑，是后端的核心处理层
- `seed/`：用于初始化知识点、题库、真题等数据

### 4.4 `data/` 目录说明

```text
data/
├── knowledge.db            SQLite 数据库文件
└── uploads/                上传后的 PDF/图片资料
```

该目录是当前项目的运行时数据中心：

- `knowledge.db` 保存系统业务数据
- `uploads/` 保存文档上传后的原始文件

### 4.5 `二工大804资料包/` 目录说明

该目录主要存放考试原始资料和学习材料，属于业务资源层，不是应用运行所必须的代码模块，但对题库建设、资料上传和知识整理具有重要价值。其内容大致包括：

- 2022-2025 年真题
- 考试大纲
- C 语言章节习题
- 专业课教材
- 数据结构资料
- 期末卷和套卷

这说明项目的业务素材基础较完整，后续可以继续把这些资料结构化导入数据库或文档系统。

## 5. 前端架构分析

### 5.1 前端入口

前端入口分为两层：

- `src/main.tsx`：挂载 React 根节点
- `src/App.tsx`：配置全局 Ant Design、中文环境、路由树

`App.tsx` 的作用非常关键，它完成了：

- 全局 UI 主题配置
- 中文本地化
- 路由入口配置
- 主布局挂载

### 5.2 路由结构

前端采用 `BrowserRouter + Routes + Route` 的形式组织页面，`MainLayout` 作为根布局组件，通过嵌套路由承载所有业务页面。

当前核心路由包括：

- `/`：仪表盘首页
- `/knowledge-map`：知识地图
- `/practice`：刷题练习
- `/ai-tutor`：AI 导师
- `/mock-exam`：模拟考试
- `/review-plan`：复习计划
- `/documents`：资料管理
- `/progress`：学习统计

### 5.3 页面层设计

#### 5.3.1 Dashboard

项目首页，通常用于展示学习概览和快速入口，是整个平台的导航起点。

#### 5.3.2 KnowledgeMap

知识地图页面承担“知识点浏览和章节导航”的核心功能。它会：

- 拉取知识点树
- 支持用户查看某个知识点详情
- 支持从知识点切换到 AI 讲解
- 支持从知识点跳转到章节练习

这意味着知识点不仅用于展示，也作为后续刷题和 AI 讲解的上下文入口。

#### 5.3.3 Practice

刷题模块是系统最核心的业务模块之一，围绕做题、判题、结果反馈和历史记录构建。当前结构包含：

- 练习配置
- 作答过程
- 结果反馈
- 历史记录

支持的筛选维度包括：

- 题型
- 学科分区
- 难度
- 知识点
- 随机题模式

#### 5.3.4 AITutor

AI 导师页面以聊天交互为主，支持：

- 普通对话
- 知识点讲解
- 答案讲评
- 流式输出

该模块是系统智能化的核心入口。

#### 5.3.5 MockExam

模拟考试页面负责生成试卷、开始考试、提交试卷和查看结果，说明项目已经从“单题练习”扩展到“整卷考试”层面。

#### 5.3.6 ReviewPlan

复习计划模块负责基于掌握度和到期时间生成待复习知识点，体现了系统从单次练习向长期记忆管理延伸。

#### 5.3.7 Documents

资料管理页面支持：

- 文件上传
- 文档列表查看
- 删除资料

与后端 OCR 能力结合后，该页面可以充当资料入库入口。

#### 5.3.8 Progress

学习统计模块负责聚合练习数据和掌握度数据，展示：

- 总刷题数
- 正确率
- 章节统计
- 学科对比
- 雷达图

它是系统“数据反馈层”的体现。

### 5.4 前端组件层

前端组件层目前有两个明显重点：

- `Layout/`：平台统一布局与导航
- `AIChat/`：AI 交互基础能力

这说明项目已经形成基础组件抽象，但当前抽象层次还不算很深，仍以页面直连业务逻辑为主。

### 5.5 前端状态管理

项目使用 Zustand，而不是 Redux，整体更轻量。

当前状态管理主要包括：

- `useAppStore`：全局状态，例如选中知识点
- `usePracticeStore`：练习过程状态，例如题目列表、当前题目、答案、结果等

这样的设计说明：

- 全局状态较少
- 页面状态以局部管理为主
- 刷题流程状态是重点保留对象

### 5.6 前端接口封装

前端通过 `services/` 目录统一封装 API 调用，包含：

- `api.ts`：Axios 实例、拦截器、统一基址
- `knowledgeApi.ts`
- `questionApi.ts`
- `progressApi.ts`
- `documentApi.ts`

其中 `api.ts` 的重要作用有：

- 将接口基础路径统一为 `/api`
- 通过拦截器统一解包后端响应结构

这说明后端接口返回并非前端直接使用的裸数据，而是经过统一包装。

## 6. 后端架构分析

### 6.1 后端入口

后端启动入口为 `backend/run.py`，通过 Uvicorn 运行 `app.main:app`。真正的应用初始化逻辑在 `backend/app/main.py` 中。

`main.py` 主要完成以下工作：

- 注册 FastAPI 应用
- 定义 lifespan 生命周期
- 启动时自动建表
- 创建上传目录
- 配置 CORS
- 挂载所有 API 路由
- 提供健康检查接口

### 6.2 生命周期与初始化

后端在应用启动时执行以下初始化动作：

1. 调用 `init_db()` 自动创建数据库表
2. 自动创建上传目录

这意味着项目具有较低的启动门槛，不需要先手工建表。

### 6.3 路由层设计

后端路由按业务模块拆分到 `api/` 目录，当前包括：

- `knowledge.py`
- `questions.py`
- `practice.py`
- `ai.py`
- `review.py`
- `exam.py`
- `documents.py`
- `progress.py`

这种拆分方式比较清晰，符合 FastAPI 项目常见的按领域划分方式。

### 6.4 服务层设计

`services/` 是后端业务核心，当前包含：

- `knowledge_service.py`
- `question_service.py`
- `ai_service.py`
- `ocr_service.py`

职责划分如下：

- `knowledge_service.py`：知识树构建与知识点查询
- `question_service.py`：题目筛选、随机抽题、判题、历史记录、错题逻辑
- `ai_service.py`：AI Provider 选择、Prompt 构建、流式输出、会话保存
- `ocr_service.py`：PDF 和图片文本提取、扫描件检测与 OCR 识别

### 6.5 AI Provider 设计

AI 模型调用没有直接写死在服务中，而是单独放入 `ai_providers/` 目录。当前已有：

- `base.py`
- `deepseek_provider.py`
- `claude_provider.py`

这说明系统采用了 Provider 抽象思路，具备以下优点：

- 可以按模型厂商切换
- 便于增加新的大模型
- 业务层不需要直接依赖某一家模型 SDK

### 6.6 配置管理

项目配置位于 `backend/app/config.py`，基于 `pydantic-settings` 读取 `.env`。

当前可配置内容主要包括：

- 应用名
- 数据库地址
- 上传目录
- CORS 白名单
- 各类 AI API Key
- 默认 AI Provider

默认数据库与上传目录均指向项目根目录下的 `data/`，适合本地开发和单机部署。

## 7. 数据库设计分析

### 7.1 数据库类型

当前数据库为 SQLite，连接方式为异步 `aiosqlite`。数据库文件位于：

```text
data/knowledge.db
```

该设计适合：

- 本地开发
- 小规模单机运行
- 快速演示
- 个人学习型项目

若未来要支持多用户、高并发或部署到生产环境，通常需要迁移到 MySQL 或 PostgreSQL。

### 7.2 ORM 基础层

`database.py` 负责：

- 创建异步引擎
- 创建 `AsyncSession`
- 提供 `get_db()` 依赖
- 提供 `init_db()` 自动建表

### 7.3 核心数据表

当前主要业务表包括：

- `knowledge_points`
- `questions`
- `question_knowledge_points`
- `practice_records`
- `knowledge_mastery`
- `documents`
- `ai_conversations`
- `mock_exams`

### 7.4 知识点模型

知识点表是整个系统的业务主轴之一，关键特点包括：

- 自关联树结构
- 使用 `parent_id` 表示父节点
- 支持章节、难度、考试权重等业务属性

它承担以下作用：

- 知识地图展示
- AI 讲解上下文
- 刷题按知识点筛选
- 复习计划关联

### 7.5 题目模型

题目表字段较完整，支持：

- 题型
- 科目分区
- 难度
- 题干
- 选项
- 答案
- 解析
- 来源
- 代码片段

支持的题型包含：

- 单选题
- 多选题
- 填空题
- 程序阅读题
- 分析题
- 计算题
- 编程题
- 简答题

这说明系统的题库模型不是简单的客观题模型，而是面向专业课综合题型设计的。

### 7.6 题目与知识点关系

题目与知识点通过 `question_knowledge_points` 中间表建立多对多关系。

这样做的优点是：

- 一个题目可以覆盖多个知识点
- 一个知识点可以关联多道题
- 便于知识点维度统计和训练

### 7.7 练习记录模型

练习记录表用于保存用户答题行为，主要记录：

- 题目 ID
- 用户答案
- 是否正确
- 用时
- 练习模式
- 创建时间

该表支撑：

- 历史记录
- 错题本
- 统计分析
- 正确率计算

### 7.8 掌握度模型

`knowledge_mastery` 表用于记录用户对某知识点的掌握情况以及复习调度参数，说明项目已经引入记忆曲线或类似间隔复习的思想。

这张表一般承担以下职责：

- 记录掌握度分数
- 记录下次复习时间
- 存放复习算法参数

### 7.9 文档模型

文档表用于保存上传文档的元数据和提取后的文本内容。它把外部资料与系统内部知识加工能力连接起来。

### 7.10 AI 对话模型

AI 对话表用于保存聊天上下文，支持以下能力：

- 保留会话记录
- 关联某个知识点
- 关联某道题目
- 复用上下文继续提问

### 7.11 模拟考试模型

模拟考试表用于保存生成的试卷、开始时间、提交时间和考试结果，是从题目训练进阶到整卷训练的基础。

## 8. 核心业务模块详解

### 8.1 知识地图模块

知识地图模块的核心逻辑在后端 `knowledge_service.py`。

它通过递归方式把知识点表构造成树形结构返回给前端。返回节点不仅包含基础字段，还包含：

- `id`
- `parent_id`
- `name`
- `description`
- `part`
- `chapter`
- `order`
- `difficulty`
- `exam_weight`
- `children`

这一模块的价值在于：

- 为前端提供树状学习路径
- 为 AI 讲解提供上下文信息
- 为练习模块提供章节入口

### 8.2 刷题模块

刷题模块是后端最完整的业务模块之一，核心逻辑在 `question_service.py`。

它覆盖以下能力：

- 条件筛选题目
- 随机抽题
- 按知识点抽题
- 创建题目
- 自动判题
- 保存练习记录
- 查询练习历史
- 获取错题集合

#### 8.2.1 判题逻辑特点

判题函数根据不同题型采用不同策略：

- 多选题：比较选项集合，支持一定条件下的部分得分
- 填空题和简答题：支持多个可接受答案，允许包含式匹配
- 编程题：做基础代码归一化比较
- 其他题型：主要做标准答案直接比较

这说明当前系统的判题逻辑已经超出简单字符串完全匹配，但对于主观题和编程题仍属于基础版本，未来还可继续增强。

### 8.3 AI 导师模块

AI 模块由 `ai.py` 路由和 `ai_service.py` 服务共同构成，是项目的智能化核心。

目前 AI 相关能力包括：

- 一般聊天
- 知识点讲解
- 题目批改与讲评
- 流式返回回答内容
- 保存对话记录

#### 8.3.1 Prompt 设计

系统在服务层内置了一段较明确的系统提示词，明确要求：

- 用中文回答
- 结合 804 考试重点
- 涉及代码时使用 C 语言
- 更偏向启发式辅导而不是直接给答案

这使得 AI 输出具有明显的考试垂直领域属性。

#### 8.3.2 会话持久化

AI 会话不是临时请求，而是会保存到数据库。这样可以支持：

- 继续对话
- 恢复上下文
- 回看历史讲解

#### 8.3.3 流式返回

AI 接口使用 SSE 输出，前端逐块消费返回内容，因此聊天体验更接近真实对话产品。

### 8.4 OCR 与文档模块

文档模块与 OCR 模块共同构成资料入库能力。

#### 8.4.1 支持的资料类型

从当前架构看，项目主要面向以下类型的学习资料：

- PDF
- 图片
- 文本文件
- Markdown
- Docx

#### 8.4.2 OCR 处理流程

OCR 服务具备较完整的双路径识别逻辑：

1. 先判断 PDF 是否为扫描件
2. 如果是文本型 PDF，则直接提取文本
3. 如果是扫描型 PDF，则逐页转图片后 OCR
4. 图片文件直接走 OCR

该设计能够兼顾：

- 文本型 PDF 的处理效率
- 扫描件资料的适配能力
- 中英文混合识别

### 8.5 模拟考试模块

模拟考试模块用于组装整套试卷，并提供开始考试和提交评分接口。说明题库已不只是供散点训练，也可用于完整考试场景。

该模块未来可继续扩展的方向包括：

- 固定题型配比
- 考试时间限制
- 答题卡
- 试卷回顾
- 历史试卷分析

### 8.6 复习计划模块

复习计划模块体现了“学习节奏管理”的能力。当前设计显示其会根据掌握度和复习到期时间生成复习列表，并支持复习后更新结果。

从架构上看，这一模块对提升用户长期留存和学习闭环非常关键。

### 8.7 学习统计模块

学习统计模块负责聚合练习记录、知识点掌握度和学科信息，对用户学习成果进行可视化反馈。

这类模块的业务意义在于：

- 让用户看到进步
- 发现薄弱章节
- 指导下一步训练方向

## 9. 前后端交互与数据流

### 9.1 知识点学习流

典型流程如下：

1. 前端知识地图页面请求知识树
2. 后端查询知识点表并递归组装树结构
3. 前端展示树和详情
4. 用户从知识点进入 AI 讲解或章节练习

### 9.2 刷题训练流

典型流程如下：

1. 前端根据筛选条件请求题目
2. 后端从题库中筛选或随机抽题
3. 前端展示题目并保存作答状态
4. 用户提交答案
5. 后端自动判题并写入练习记录
6. 前端展示正确答案、解析和得分结果
7. 后续统计与错题模块复用这些记录

### 9.3 AI 学习流

典型流程如下：

1. 用户在 AI 页面输入问题，或从知识点/题目进入 AI 讲解
2. 前端向 AI 接口发起流式请求
3. 后端根据 provider 构造消息并请求大模型
4. 模型流式输出内容
5. 前端逐步渲染回答
6. 完整对话写入数据库

### 9.4 文档入库流

典型流程如下：

1. 用户上传 PDF 或图片
2. 后端保存原始文件到 `data/uploads/`
3. 后端判断是否需要 OCR
4. 提取文本并写入文档表
5. 前端可查看文档列表和文档内容

### 9.5 复习调度流

典型流程如下：

1. 系统查询已到期的复习知识点
2. 前端展示待复习列表
3. 用户完成复习并提交结果
4. 后端更新掌握度与下一次复习时间

## 10. 关键接口模块说明

后端已经按业务拆分了多个接口模块，整体接口边界比较清晰。

### 10.1 知识点接口

主要负责：

- 获取知识点树
- 获取单个知识点详情

### 10.2 题目接口

主要负责：

- 获取题目列表
- 按条件筛选
- 随机抽题

### 10.3 练习接口

主要负责：

- 提交答案
- 获取练习历史
- 获取错题
- 获取练习统计

### 10.4 AI 接口

主要负责：

- AI 聊天
- 知识点讲解
- 答案批改

### 10.5 复习接口

主要负责：

- 获取待复习知识点
- 提交复习结果

### 10.6 模拟考试接口

主要负责：

- 生成试卷
- 开始考试
- 提交试卷

### 10.7 文档接口

主要负责：

- 上传文档
- 获取文档列表
- 删除文档

### 10.8 统计接口

主要负责：

- 获取学习概览
- 获取章节统计
- 获取进度详情

## 11. 运行与开发方式

### 11.1 前端启动

```bash
cd frontend
npm install
npm run dev
```

默认端口为 `5173`。

### 11.2 后端启动

```bash
cd backend
pip install -r requirements.txt
python run.py
```

默认端口为 `8000`。

### 11.3 前后端联调方式

前端通过 Vite 代理将 `/api` 转发到后端，因此本地开发时推荐同时启动：

- 前端：`http://localhost:5173`
- 后端：`http://localhost:8000`

### 11.4 数据初始化

后端提供多个种子脚本，用于导入知识点和题库数据，例如：

- `seed_knowledge.py`
- `seed_questions.py`
- `seed_knowledge_extended.py`
- `seed_questions_extended.py`
- `seed_2022_2023_exam.py`
- `seed_2024_exam.py`
- `seed_ds_exercises.py`
- `seed_ds_papers.py`

这说明项目已经具备进一步扩充题库和真题资料的基础能力。

## 12. 项目优势分析

### 12.1 业务闭环较完整

从知识学习到刷题训练，再到 AI 讲解、复习计划和统计反馈，项目已经初步形成完整学习闭环。

### 12.2 领域聚焦明确

项目聚焦 804 专业课，而不是做泛化题库平台，因此更容易深入打磨知识点体系与内容质量。

### 12.3 架构扩展性较好

虽然整体结构不复杂，但已经具备清晰的模块边界。尤其是：

- 路由与服务分层
- AI Provider 抽象
- 知识点与题目的多对多设计
- 文档 OCR 独立服务化

这些设计都为后续迭代留出了空间。

### 12.4 本地启动门槛低

使用 SQLite、本地文件上传和自动建表，便于快速启动、测试和演示。

## 13. 当前项目的不足与改进建议

以下内容是基于当前代码结构得出的工程观察，不影响项目已有价值，但值得后续优化。

### 13.1 数据库层仍偏单机

当前使用 SQLite，适合开发与轻量场景，但若后续考虑多用户或部署线上，建议迁移到更成熟的数据库。

### 13.2 鉴权能力尚未体现

从当前代码结构看，尚未发现用户系统、登录鉴权、权限控制等模块，因此目前更像单用户本地学习系统。

### 13.3 生产部署能力不足

项目当前更偏开发态：

- 没有明显的 Docker 配置
- 没有完整生产部署脚本
- 后端启动脚本仍使用 `reload=True`

如需正式上线，建议补充部署方案。

### 13.4 主观题智能评测仍可增强

现有判题逻辑对客观题和简单文本比较支持较好，但对编程题、分析题、简答题的语义判分仍有较大提升空间。

### 13.5 前端模块化还可继续提升

当前页面层承载的业务逻辑相对较多，后续可以进一步抽离：

- 自定义 hooks
- 业务组件
- 统一查询层
- 更完整的错误处理

### 13.6 文档知识化能力可继续增强

当前文档模块已具备 OCR 和文本提取，但如果要进一步打造真正的知识库，还可以增加：

- 文档分段切片
- 标签系统
- 向量检索
- 基于文档的问答

## 14. 后续演进建议

结合当前项目基础，后续建议优先往以下方向推进：

### 14.1 完善用户体系

- 登录注册
- 学习记录按用户隔离
- 用户个性化复习计划

### 14.2 强化题库能力

- 更丰富的题型评分策略
- 题目收藏与错题重做
- 题目质量标注
- 真题试卷结构化导入

### 14.3 强化 AI 学习助手

- 基于知识点上下文自动引用题目
- 基于历史错题生成针对性讲解
- 支持答案追问
- 支持复习建议和阶段总结

### 14.4 提升知识库能力

- 文档切片
- 知识点与资料自动关联
- 关键词检索
- 语义搜索

### 14.5 提升工程化水平

- 增加测试
- 增加 Docker
- 区分开发与生产配置
- 增加日志与异常监控
- 增加数据库迁移规范

## 15. 总结

从当前代码来看，本项目已经不是一个简单的 CRUD 练手项目，而是一个具备明确业务目标、较完整学习闭环和可持续扩展能力的考研专业课系统。

它的核心优势在于：

- 专业课场景聚焦明确
- 知识点、题库、AI、OCR、统计、复习形成联动
- 前后端职责清晰
- 本地启动方便
- 后续扩展空间大

如果把它定义为一个“面向 804 考研的智能学习平台雏形”，这个评价是比较准确的。当前版本已经具备良好的架构基础，后续只要继续完善数据质量、用户体系、智能评测和部署能力，就可以进一步提升为更成熟的学习产品。

