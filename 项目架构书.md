# 考研804知识库系统项目架构书

## 1. 项目定位

考研804知识库系统是一个面向上海第二工业大学 804《数据结构与高级程序设计》的专题学习平台。项目围绕"知识点学习、章节刷题、错题复习、AI 辅导、模拟考试、资料管理、学习统计"构建学习闭环。

系统已演进为支持多用户的 JWT 认证学习平台：前端为 React SPA，后端为 FastAPI API 服务，数据存储使用 SQLite，本地资料和上传文件保存在项目 data/ 目录。生产部署通过 Render Web Service 完成。

## 2. 业务边界

项目服务的核心对象是 804 专业课考生，内容边界是：

- 高级程序设计，主要对应 C 语言
- 数据结构
- 804 考纲、真题、题库、章节练习和复习资料

主要业务能力：

- 知识点树和知识地图
- 按章节、题型、科目刷题
- 错题记录与错题复习
- AI 导师问答、知识点讲解、答案点评
- AI 回答反馈与 few-shot 示例沉淀
- 模拟考试（含题型不足检测）
- 文档上传、文本提取和 OCR
- 学习进度统计（概览、章节掌握度、雷达图）
- 每日学习指挥台（Dashboard）
- 用户注册登录（JWT + 邀请码机制）
- 管理员后台（用户管理、邀请码生成）

当前不重点覆盖：

- 在线支付或商业权限
- 大规模并发

## 3. 总体架构

`text
+---------------------------------------------+
|                 React 前端 SPA              |
|  Ant Design / Zustand / Router / API Service |
+----------------------+----------------------+
                       | /api
                       v
+---------------------------------------------+
|                 FastAPI 后端                 |
|  API Router / Service / AI Provider / OCR    |
+---------------+-------------+---------------+
                |             |
                v             v
+-------------------------+  +---------------------------+
| SQLite: data/knowledge.db|  | DeepSeek / Claude / OCR   |
| 业务表、练习记录、AI记录  |  | AI生成、文档识别           |
+-------------+-----------+  +---------------------------+
              |
              v
+-------------------------+
| data/uploads/            |
| 上传资料、PDF、图片、文本 |
+-------------------------+
`

架构特征：

- 前后端分离开发，生产环境由 FastAPI 同源服务前端构建产物。
- 后端是单体 API 服务，按业务模块拆分 router 和 service。
- 数据库使用 SQLite async，适合本地学习系统和轻量部署。
- AI Provider 采用懒加载模式，Claude 依赖缺失不会阻塞启动。
- JWT 认证中间件（dependencies.py）统一管理密码哈希、Token 签发/验证、权限守卫。

## 4. 代码目录架构

`text
考研知识库系统/
├── frontend/                 前端应用
│   └── src/
│       ├── App.tsx           主题、路由入口
│       ├── components/       Layout、AIChat、MarkdownCode
│       └── pages/            Dashboard、Study、Review、AITutor、
│                              MockExam、Documents、Login、Register、Admin
├── backend/                  后端应用
│   └── app/
│       ├── main.py           应用入口、lifespan、SPA fallback
│       ├── config.py         Pydantic v2 配置（含 JWT）
│       ├── database.py       数据库引擎 + 迁移
│       ├── dependencies.py   JWT 认证中间件
│       ├── migrations.py     轻量级迁移机制
│       ├── time_utils.py     时间工具函数
│       ├── api/              10个路由模块
│       ├── models/           13个ORM模型
│       ├── schemas/          Pydantic schema
│       ├── services/         业务逻辑层
│       └── ai_providers/     DeepSeek + Claude Provider
├── data/                     数据库和上传文件
├── scripts/                  数据导入、清洗、迁移脚本
├── skills/                   已安装的 skills
├── docs/                     项目辅助文档
├── render.yaml               Render 部署配置
└── start.py                  本地一键启动脚本
`

## 5. 前端架构

### 5.1 技术组成

- React 19 + TypeScript
- Vite 8（使用 Rolldown，配置 codeSplitting 分包）
- Ant Design 6（Indigo 色系主题）
- React Router 7（lazy 懒加载）
- Zustand（全局状态 + 刷题流程状态 + 认证状态）
- Axios（统一拦截器）
- Recharts（学习统计图表）
- React Markdown + 自定义 MarkdownCode 组件
- Monaco Editor（代码编辑）
- vite-plugin-pwa（PWA 支持）

### 5.2 认证与路由守卫

App.tsx 引入 AuthGuard 和 AdminGuard：
- AuthGuard：检查 useAuthStore.isAuthenticated，未登录重定向到 /login
- AdminGuard：在 AuthGuard 基础上验证 useAuthStore.isAdmin，非管理员重定向到 /
- 独立路由 /login 和 /register 不受守卫限制

### 5.3 启动入口

`text
frontend/src/main.tsx
  └── frontend/src/App.tsx
        └── frontend/src/components/Layout/MainLayout.tsx
              └── KeepAlive 包裹各页面（display:none/block 切换）
`

App.tsx 负责：
- 注入 Ant Design 中文 locale
- 全局主题 token（Indigo #6366F1 主色）
- 全局组件样式定制（圆角按钮、卡片、表格等）
- React Router 入口

### 5.4 导航与路由（6项核心 + 1项管理）

当前导航为 4 项核心功能 + 1 项管理员专属，多个功能通过 Tab 整合。Admin 页面已集成到 MainLayout 中（非独立页面），侧边栏在所有页面保持可见。

| 导航项 | 路径 | 懒加载页面 | Tab整合 |
|--------|------|-----------|---------|
| 首页 | / | Dashboard | — |
| 学习 | /study | Study | 知识地图 + 刷题 + 错题 |
| 复习 | /review | Review | 复习计划 + 学习统计 |
| 模拟考试 | /mock-exam | MockExam | — |
| 管理 (仅管理员) | /admin | Admin | 用户管理 + 邀请码 |

> 注：AI助手和资料库导航项已隐藏，但后端 API 和后端路由仍保留，可通过 URL 直接访问。登录/注册页采用浅色动态背景（CSS 动画浮动 blob + 粒子光点），已移除学校名称。

路由实现：
- 独立路由（不通过 MainLayout）：
  - /login — 登录页
  - /register — 注册页（需邀请码）
- /* catch-all → AuthGuard → MainLayout（含所有页面，包括 /admin）

所有页面通过 React.lazy(() => import(...)) 懒加载
- MainLayout 使用 KeepAlive 同时挂载所有页面（含 AdminPage）
- 通过 display: none/block 保留页面状态，避免切换丢失
- Admin 页面非独立路由，集成在 MainLayout pages 数组中，按 isAdmin 过滤
- 移动端通过 Drawer 展示侧边栏

### 5.4 核心页面

**Dashboard（学习仪表盘）**
- 每日学习指挥台（Hero区）：今日目标进度条、待复习数、优先章节
- 统计卡片：总练习次数、正确次数、正确率、知识点章节数
- 优先补弱章节列表 + 章节掌握度快览
- 数据来源：/api/progress/overview + /api/progress/detail

**Study（学习中心）**
- Tab1 知识地图：知识点树形展示，支持 AI 讲解
- Tab2 刷题练习：章节/随机/错题三种模式
- Tab3 错题记录：错题列表，回顾重做

**Review（复习进度）**
- Tab1 复习计划：到期知识点列表，按掌握度排序
- Tab2 学习统计：正确率趋势、章节掌握度雷达图

**Admin（管理后台，仅管理员可见）**
- 集成在 MainLayout 侧边栏内，非独立页面
- Tab1 用户管理：用户列表、删除用户
- Tab2 邀请码：生成邀请码、查看/删除邀请码列表

### 5.5 状态管理

- useAppStore（Zustand）：全局选中知识点、当前章节
- usePracticeStore（Zustand）：刷题流程状态（题目列表、当前题号、答案）
- useAuthStore（Zustand）：认证状态（token/user/isAdmin/isAuthenticated/loading/login/logout/init）

### 5.6 API 服务层

- services/api.ts：axios 实例，baseURL: '/api'，统一拦截器
- services/knowledgeApi.ts：知识点 API
- services/questionApi.ts：题目与练习 API（题目列表、随机题、章节统计、提交答案、练习历史、错题）
- services/progressApi.ts：学习进度 API（概览、章节详情、雷达图）
- services/documentApi.ts：资料管理 API（上传、列表、详情、内容、删除）
- services/systemApi.ts：系统健康检查 API
- services/authApi.ts：登录/注册 API
- services/adminApi.ts：管理员 API（用户管理、邀请码）

## 6. 后端架构

### 6.1 技术组成

- FastAPI（异步 API 框架）
- SQLAlchemy 2.0（async ORM）
- SQLite + aiosqlite（数据库）
- Uvicorn（ASGI 服务器）
- Pydantic v2 + pydantic-settings（配置管理）
- python-jose + passlib[pbkdf2_sha256]（JWT 认证，生产环境兼容 bcrypt 已存密码）

### 6.2 模块划分

`text
backend/app/
├── main.py              应用入口、lifespan、CORS、SPA fallback、Admin 自动创建
├── config.py            Settings (Pydantic v2 model_config, JWT 配置)
├── database.py          engine/session/Base/init_db + run_migrations
├── dependencies.py      JWT 认证中间件 (hash/verify/create_token/get_current_user/require_admin)
├── migrations.py        轻量级迁移（schema_migrations 表）
├── time_utils.py        UTC 时间工具
├── api/                 10个路由模块
│   ├── knowledge.py     知识点 CRUD + 树形结构
│   ├── questions.py     题目 CRUD + 随机题 + 章节统计
│   ├── practice.py      提交答案、练习历史、错题、统计
│   ├── ai.py            AI 聊天(SSE)、讲解、点评、反馈
│   ├── review.py        待复习、提交复习、统计
│   ├── exam.py          生成试卷、开始/提交考试、列表
│   ├── documents.py     上传、列表、详情、内容、删除
│   ├── progress.py      概览、章节详情、雷达图
│   ├── auth.py          登录、注册（邀请码验证）
│   └── admin.py         用户管理、邀请码管理
├── models/              13个ORM模型
├── schemas/             common/knowledge/question
├── services/            ai_service/question_service/knowledge_service/feedback_service/ocr_service
└── ai_providers/        base/deepseek_provider/claude_provider(懒加载)
`

### 6.3 启动流程

`text
1. uvicorn.run("app.main:app")
2. lifespan 启动:
   a. init_db()
      - Base.metadata.create_all (建表)
      - run_migrations(conn) (迁移，失败只记日志不阻断)
   b. os.makedirs(uploads)
   c. seed_all() (首次自动填充数据，已存在则跳过)
   d. 自动创建 admin 账户 (admin/admin123)
3. 注册路由 (10个 API 模块)
4. 生产环境: 挂载 frontend/dist，SPA fallback
`

### 6.4 迁移机制

- migrations.py：管理 schema_migrations 表
- Migration dataclass：version / description / statements
- run_migrations(conn)：幂等执行，已应用版本跳过
- time_utils.py：utc_now_naive() 记录迁移时间
- 迁移失败不阻断应用启动（try/except 保护）

### 6.5 AI Provider 架构

- **Provider 接口**：base.py 定义 chat_stream() 抽象方法
- **DeepSeekProvider**：默认 Provider（模型 `deepseek-chat`），依赖 openai 包，顶层导入
- **ClaudeProvider**：懒加载（模型 `claude-sonnet-4-6`），get_provider() 函数内部 import，依赖 anthropic 包
- **Fallback**：未配置 Key 时返回友好错误提示
- **Few-shot**：从 ai_training_examples 表检索相关示例

### 6.6 文档处理

- 支持格式：PDF / DOCX / TXT / MD / PNG / JPG
- 文本文件：多编码尝试（UTF-8 → GB18030 → GBK → fallback）
- DOCX：python-docx 提取段落文本
- PDF：pdfplumber 提取文本；扫描件检测（前5页 CJK 字符密度 < 50）后通过 PyMuPDF 渲染为图像，easyocr（ch_sim + en）OCR 识别
- OCR 依赖按需懒加载（`_get_reader()` 单例），避免阻塞启动
- 上传文件存储：data/uploads/，UUID 防冲突文件名

## 7. 数据库表结构

| 表名 | 用途 | 关键字段 |
|------|------|---------|
| users | 用户账户 | username, password_hash, role |
| invite_codes | 邀请码 | code, created_by, used_by, is_used |
| knowledge_points | 知识点树 | name, part, chapter, difficulty, parent_id |
| questions | 题库 | type, part, difficulty, content, answer |
| question_knowledge_points | 题目-知识点关联 | question_id, knowledge_point_id |
| practice_records | 练习记录 | user_id, question_id, user_answer, is_correct |
| knowledge_mastery | 掌握度 | knowledge_point_id, mastery_level, next_review_at |
| documents | 上传文档 | filename, file_type, content_text |
| ai_conversations | AI对话 | provider, messages(JSON) |
| ai_feedbacks | 用户反馈 | conversation_id, rating, comment |
| ai_training_examples | Few-shot示例 | user_question, assistant_answer, chapter |
| mock_exams | 模拟考试 | user_id, questions(JSON), answers, score |
| schema_migrations | 迁移记录 | version, description, applied_at |

## 7.5 用户认证流程

`text
1. 管理员通过 /api/admin 创建邀请码
2. 新用户通过 /register 使用邀请码注册
3. 注册成功 → 邀请码标记为已使用 → 返回 JWT token
4. 后续请求携带 Authorization: Bearer <token>
5. dependencies.get_current_user 解析 JWT 并返回 User
6. 管理员接口额外通过 require_admin 验证 role
`

## 7.6 Provider 懒加载机制

AI Provider 采用分层导入策略，避免缺少依赖时应用启动崩溃：

- **DeepSeekProvider**：顶层导入（`openai` 包为必需依赖），作为默认 Provider
- **ClaudeProvider**：懒加载模式，`get_provider("claude")` 内部动态 `import anthropic`，依赖缺失时返回友好错误提示而非崩溃
- **OCR Reader**：`_get_reader()` 采用模块级单例 + 首次调用时初始化 easyocr，避免 Render 部署时加载重型 OCR 依赖
- **配置驱动**：通过 `DEFAULT_AI_PROVIDER` 环境变量切换默认 Provider，前端 ModelSelector 支持运行时选择

## 8. 关键业务流程

### 8.1 刷题流程

`text
Study(Practice Tab)
  -> 选择模式(章节/随机/错题)
  -> GET /api/questions (question_service 筛选)
  -> 注入 usePracticeStore
  -> 逐题作答
  -> POST /api/practice/submit
  -> 记录 practice_records + 更新 knowledge_mastery
  -> 错题自动加入错题列表
`

### 8.2 AI 辅导流程

`text
AITutor / Study / Practice
  -> POST /api/ai/chat 或 explain 或 review-answer
  -> ai_service 组装上下文 + Few-shot 示例 + prompt
  -> DeepSeek/Claude Provider
  -> SSE 流式返回
  -> 保存 ai_conversations
  -> 用户可提交反馈 -> 沉淀训练样例
`

### 8.3 模拟考试流程

`text
MockExam 页面
  -> POST /api/exam/generate
  -> 题型不足检测（8个section最低题目数）
  -> POST /api/exam/{id}/start
  -> 用户作答
  -> POST /api/exam/{id}/submit
`

## 9. 部署架构

Render 部署配置位于 render.yaml，使用 Python runtime：

- **构建**：pip install -r backend/requirements.txt → cd frontend && npm ci && npm run build
- **启动**：cd backend && python run_prod.py
- **环境变量**：PORT=8000, ENABLE_CORS=false
- **生产行为**：FastAPI 服务 frontend/dist，SPA fallback

### 已修复的部署问题

| 提交 | 问题 | 修复 |
|------|------|------|
| 3ae069f | run_migrations 异常导致启动崩溃 | 添加 try/except 保护 |
| 292c900 | ClaudeProvider 顶层导入崩溃 | 改为懒加载 |
| 399d038 | anthropic 未写入依赖 | 添加 anthropic>=0.39 |
| 9e80fc5 | Render 部署 admin 登录 401 | bcrypt → pbkdf2_sha256（纯 Python，无需 C 编译） |
| 7b858bd | 本地 502 + 登录错误消息丢失 | 端口自动清理 + API 拦截器保留服务端 message |
| 705c790 | Render 用户数据被清空 | seed_all 改为 ATTACH+INSERT 安全导入，保护 users 表 |

## 10. 本地开发

`powershell
python start.py                          # 一键启动
cd backend && python run.py              # 后端 (port 8000)
cd frontend && npm run dev               # 前端 (port 5173)
http://localhost:8000/docs               # Swagger
`

## 11. 工程约束与风险

- **数据库**：SQLite 适合当前场景，多用户/高并发需迁移到 PostgreSQL。
- **用户体系**：已支持多用户 JWT 认证 + 邀请码注册，APP_MODE 配置保留向后兼容。
- **AI Key**：通过环境变量或 .env 注入，不提交仓库。
- **OCR 依赖**：重型依赖已从 Render 构建移除，改为按需懒加载。
- **前端构建**：Vite 8 + Rolldown，codeSplitting 分包。
- **迁移机制**：轻量级实现，后续可演进为 Alembic。

## 12. 当前状态与演进建议

**当前状态（2026-06-30）：**
- 导航精简为 4 项核心功能 + 1 项管理员专属（AI助手、资料库已隐藏）
- Admin 页面已集成到 MainLayout，侧边栏全局可见
- 登录/注册页面：浅色动态背景（CSS blob + 粒子动画），已移除学校名称
- Vite 文件监听已排除 data/、backend/ 目录
- 所有 AI 端点 fetch 调用已补全 JWT Authorization header
- 密码哈希主方案改为 pbkdf2_sha256（修复 Render 部署 bcrypt 不可用导致 admin 创建失败）
- API 拦截器：所有非零业务码统一 reject 并保留服务端 message，Login/Register 兼容 Error 对象
- start.py 启动前自动清理端口 8000/5173 残留进程；run.py 端口占用检测提示
- seed_all 安全导入：有用户但无知识点时仅 ATTACH+INSERT 种子表，不再覆盖整个数据库
- main.py 启动日志打印用户数和知识点数 `[Startup] DB at ...: N users, M knowledge points`

**后续演进建议：**

1. 扩展端到端测试覆盖
2. 迁移机制演进为 Alembic
3. 文档切片和语义检索
4. 编程题 AI 辅助评分
5. 从 SQLite 迁移到 PostgreSQL
