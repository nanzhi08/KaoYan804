# 考研804知识库系统项目架构书

## 1. 项目定位

考研804知识库系统是一个面向上海第二工业大学 804《数据结构与高级程序设计》的专题学习平台。项目围绕“知识点学习、章节刷题、错题复习、AI 辅导、模拟考试、资料管理、学习统计”构建学习闭环。

系统当前更接近个人或小团队使用的单体学习应用：前端为 React SPA，后端为 FastAPI API 服务，数据存储使用 SQLite，本地资料和上传文件保存在项目 `data/` 目录。生产部署通过 Render Web Service + 持久化磁盘完成。

## 2. 业务边界

项目服务的核心对象是 804 专业课考生，内容边界是：

- 高级程序设计，主要对应 C 语言
- 数据结构
- 804 考纲、真题、题库、章节练习和复习资料

主要业务能力：

- 知识点树和知识地图
- 按章节、题型、科目刷题
- 错题记录与错题复习
- AI 导师问答、知识点讲解、答案点评
- AI 回答反馈与 few-shot 示例沉淀
- 模拟考试
- 文档上传、文本提取和 OCR
- 学习进度统计
- 资料和题库导入脚本

当前不重点覆盖：

- 多用户账号体系
- 在线支付或商业权限
- 大规模并发
- 完整后台管理系统

## 3. 总体架构

```text
┌─────────────────────────────────────────────┐
│                 React 前端 SPA              │
│  Ant Design / Zustand / Router / API Service │
└──────────────────────┬──────────────────────┘
                       │ /api
                       ▼
┌─────────────────────────────────────────────┐
│                 FastAPI 后端                 │
│  API Router / Service / AI Provider / OCR    │
└───────────────┬───────────────┬─────────────┘
                │               │
                ▼               ▼
┌─────────────────────────┐  ┌──────────────────────────┐
│ SQLite: data/knowledge.db│  │ DeepSeek / OCR / MCP 工具 │
│ 业务表、练习记录、AI记录  │  │ AI生成、文档识别、代码审查 │
└───────────────┬─────────┘  └──────────────────────────┘
                │
                ▼
┌─────────────────────────┐
│ data/uploads/            │
│ 上传资料、PDF、图片、文本 │
└─────────────────────────┘
```

架构特征：

- 前后端分离开发，生产环境可由 FastAPI 同源服务前端构建产物。
- 后端是单体 API 服务，按业务模块拆分 router 和 service。
- 数据库使用 SQLite async，适合本地学习系统和轻量部署。
- AI、OCR、MCP 是外部能力层，围绕学习、资料处理和代码审查增强项目。

## 4. 代码目录架构

```text
考研知识库系统/
├── frontend/                 前端应用
├── backend/                  后端应用
├── data/                     数据库和上传文件
├── scripts/                  数据导入、清洗、OCR、AI缓存脚本
├── seed_data/                种子或辅助数据
├── screenshots/              页面截图验证结果
├── skills/                   Superpowers skills
├── .claude/                  Claude Code 本地配置
├── .cursor/                  Cursor MCP 配置
├── .vscode/                  VS Code MCP 配置
├── docs/                     项目辅助文档
├── 二工大804资料包/           原始考试资料
├── CLAUDE.md                 Claude 项目工作指南
├── 项目详细说明书.md          历史说明文档
├── 项目架构书.md             本文档
├── render.yaml               Render 部署配置
└── start.py                  本地一键启动脚本
```

## 5. 前端架构

### 5.1 技术组成

前端使用：

- React 19
- TypeScript
- Vite 8
- Ant Design 6
- React Router 7
- Zustand
- Axios
- Recharts
- React Markdown
- Monaco Editor
- vite-plugin-pwa

### 5.2 启动入口

前端入口链路：

```text
frontend/src/main.tsx
  └── frontend/src/App.tsx
        └── frontend/src/components/Layout/MainLayout.tsx
              └── KeepAlive 包裹各页面
```

`App.tsx` 负责：

- 注入 Ant Design 中文 locale
- 配置全局 design token
- 挂载 BrowserRouter
- 将所有路径交给 `MainLayout`

`MainLayout.tsx` 负责：

- 桌面端固定左侧导航
- 移动端 Drawer 导航
- 页面顶部 Header
- 页面列表渲染
- 使用 `KeepAlive` 保留页面状态

### 5.3 页面模块

当前前端页面主要包括：

| 页面 | 路径 | 作用 |
|------|------|------|
| Dashboard | `/` | 学习仪表盘和概览入口 |
| KnowledgeMap | `/knowledge-map` | 知识点树、知识点详情、AI讲解入口 |
| Practice | `/practice` | 刷题练习、章节浏览、提交答案 |
| WrongRecords | `/wrong-records` | 错题记录和错题复习入口 |
| AITutor | `/ai-tutor` | AI 导师聊天 |
| AIHistory | `/ai-history` | 历史 AI 回答 |
| MockExam | `/mock-exam` | 模拟考试 |
| Documents | `/documents` | 资料上传和管理 |
| ReviewPlan | `/review-plan` | 复习计划和待复习知识点 |
| Progress | `/progress` | 学习进度统计，已出现在主菜单 |

### 5.4 状态管理

前端使用 Zustand：

- `useAppStore`：保存全局选中状态，例如当前知识点。
- `usePracticeStore`：保存刷题流程状态，例如题目列表、当前题目、用户答案和练习结果。

该设计适合当前项目规模，避免引入过重的 Redux 式样板代码。

### 5.5 API 访问层

`frontend/src/services/api.ts` 创建 axios 实例：

- `baseURL: '/api'`
- `timeout: 60000`
- response interceptor 返回 `response.data`
- error interceptor 使用 Ant Design message 展示错误

业务 API 按模块拆分：

- `knowledgeApi.ts`
- `questionApi.ts`
- `progressApi.ts`
- `documentApi.ts`
- `api.ts` 中还包含 AI feedback 和 training example API

开发环境由 `vite.config.ts` 将 `/api` 代理到 `http://localhost:8000`。生产环境中 FastAPI 服务前端静态文件，前后端同源。

### 5.6 UI 设计系统

当前 UI 主题以“智识空间”为基调：

- 主色：`#6366F1`
- 成功/数据结构色：`#10B981`
- 错误/高频提醒色：`#EF4444`
- 警告色：`#F59E0B`
- 页面背景：`#F8FAFC`
- 侧边栏：`#0F172A`

Ant Design token 在 `App.tsx` 中集中配置，组件级覆盖包含 Button、Card、Table、Menu、Statistic、Tag、Progress、Modal、Input、Select、Tabs。

## 6. 后端架构

### 6.1 技术组成

后端使用：

- FastAPI
- SQLAlchemy 2.0 async
- aiosqlite
- Pydantic Settings
- Uvicorn
- httpx / openai SDK
- python-multipart
- aiofiles

`backend/requirements.txt` 当前保持较轻量，生产部署不强依赖 EasyOCR 这类重依赖。

### 6.2 启动入口

开发入口：

```text
backend/run.py
```

生产入口：

```text
backend/run_prod.py
```

FastAPI 应用入口：

```text
backend/app/main.py
```

`main.py` 主要职责：

- 创建 FastAPI app
- lifespan 中执行 `init_db()`
- 创建上传目录
- 尝试自动执行 `seed_all()`
- 配置 CORS
- 注册 API router
- 提供 `/api/health`
- 检测 `frontend/dist` 并提供 SPA fallback

### 6.3 配置管理

配置文件：

```text
backend/app/config.py
```

关键配置：

- `DATA_DIR`：数据目录，本地默认为项目根目录 `data/`
- `DATABASE_URL`：由 `DATA_DIR/knowledge.db` 自动生成
- `UPLOAD_DIR`：由 `DATA_DIR/uploads` 自动生成
- `ENABLE_CORS`：开发模式启用，生产同源部署可关闭
- `DEEPSEEK_API_KEY`
- `DEFAULT_AI_PROVIDER`
- `ENABLE_FEW_SHOT`
- `MAX_FEW_SHOT_EXAMPLES`

### 6.4 路由层

后端 API 模块：

| 模块 | prefix | 作用 |
|------|--------|------|
| `knowledge.py` | `/api/knowledge-points` | 知识点树和详情 |
| `questions.py` | `/api/questions` | 题库列表、随机题、章节统计、增删题 |
| `practice.py` | `/api/practice` | 提交答案、历史、错题、练习统计 |
| `ai.py` | `/api/ai` | 聊天、讲解、点评、反馈、训练样例 |
| `review.py` | `/api/review` | 复习计划 |
| `exam.py` | `/api/exam` | 模拟考试 |
| `documents.py` | `/api/documents` | 文档上传、列表、详情、删除 |
| `progress.py` | `/api/progress` | 学习进度和雷达图 |

### 6.5 服务层

服务层位于：

```text
backend/app/services/
```

核心服务：

- `knowledge_service.py`：知识点树构建和知识点查询
- `question_service.py`：题目筛选、随机抽题、判题、练习记录、错题
- `ai_service.py`：AI provider 调用、prompt、SSE、对话保存、讲解缓存
- `ocr_service.py`：文档文本提取、PDF/图片 OCR
- `feedback_service.py`：AI 反馈和训练样例

设计原则是 router 处理 HTTP 入参出参，service 承载业务逻辑。

### 6.6 AI Provider

AI Provider 位于：

```text
backend/app/ai_providers/
```

当前结构：

- `base.py`
- `deepseek_provider.py`
- `claude_provider.py`
- `__init__.py`

业务层通过 provider 抽象调用模型，降低对具体厂商 API 的耦合。当前配置默认 provider 是 `deepseek`。

## 7. 数据架构

### 7.1 数据存储

数据文件：

```text
data/knowledge.db
```

上传文件：

```text
data/uploads/
```

数据库通过 SQLAlchemy async engine 访问，启动时执行 `Base.metadata.create_all` 自动建表。

### 7.2 核心表

| 表 | 模型 | 作用 |
|----|------|------|
| `knowledge_points` | `KnowledgePoint` | 804 知识点树 |
| `questions` | `Question` | 题库 |
| `question_knowledge_points` | `QuestionKnowledgePoint` | 题目和知识点多对多 |
| `practice_records` | `PracticeRecord` | 练习记录 |
| `knowledge_mastery` | `KnowledgeMastery` | 知识点掌握度和复习调度 |
| `documents` | `Document` | 上传资料元数据和提取文本 |
| `ai_conversations` | `AIConversation` | AI 对话历史 |
| `ai_feedbacks` | `AIFeedback` | AI 回答反馈 |
| `ai_training_examples` | `AITrainingExample` | few-shot 训练样例 |
| `mock_exams` | `MockExam` | 模拟考试记录 |

### 7.3 知识点模型

`KnowledgePoint` 使用 `parent_id` 自关联形成树结构。关键字段：

- `name`
- `description`
- `part`
- `chapter`
- `order`
- `difficulty`
- `exam_weight`
- `ai_explanation`

知识点是系统主轴，连接知识地图、AI 讲解、刷题、复习计划和进度统计。

### 7.4 题目模型

`Question` 支持 804 专业课常见题型：

- `single_choice`
- `multi_choice`
- `fill_blank`
- `program_reading`
- `analysis`
- `calculation`
- `programming`
- `short_answer`

关键字段：

- `part`
- `difficulty`
- `content`
- `options`
- `answer`
- `explanation`
- `source`
- `code_snippet`

题目通过 `question_knowledge_points` 关联多个知识点。

## 8. 核心业务流程

### 8.1 知识点学习流程

```text
前端 KnowledgeMap
  -> GET /api/knowledge-points
  -> knowledge_service 组装树
  -> 用户查看知识点详情
  -> 可进入 AI 讲解或章节练习
```

### 8.2 刷题练习流程

```text
Practice 页面选择题目范围
  -> GET /api/questions 或 /api/questions/random
  -> 用户作答
  -> POST /api/practice/submit
  -> question_service 判题
  -> 写入 practice_records
  -> 更新错题、统计和掌握度相关数据
```

### 8.3 错题复习流程

```text
WrongRecords 页面
  -> GET /api/practice/wrong-questions
  -> 用户选择复习错题
  -> 注入 usePracticeStore
  -> 跳转 Practice 页面继续练习
```

### 8.4 AI 辅导流程

```text
AITutor / KnowledgeMap / Practice
  -> POST /api/ai/chat 或 /api/ai/explain 或 /api/ai/review-answer
  -> ai_service 组装上下文和 prompt
  -> DeepSeek Provider
  -> SSE 流式返回
  -> 保存 ai_conversations
  -> 用户可提交反馈
  -> feedback_service 沉淀训练样例
```

### 8.5 文档入库流程

```text
Documents 页面上传文件
  -> POST /api/documents/upload
  -> 保存原始文件到 data/uploads/
  -> 根据类型提取文本或 OCR
  -> 写入 documents 表
  -> 前端查看列表、详情和文本内容
```

### 8.6 模拟考试流程

```text
MockExam 页面
  -> POST /api/exam/generate
  -> 生成试卷
  -> POST /api/exam/{id}/start
  -> 用户作答
  -> POST /api/exam/{id}/submit
  -> 保存考试结果
```

## 9. MCP 与自动化能力

项目已配置 MCP：

- `tavily`：网络搜索
- `paddleocr`：PaddleOCR 即席识别
- `git`：代码仓库上下文、diff、历史和代码审查

相关配置：

- `.mcp.json`：Claude Code 项目 MCP
- `.cursor/mcp.json`：Cursor MCP
- `.vscode/mcp.json`：VS Code MCP
- `docs/MCP_CODE_REVIEW.md`：MCP 使用说明

项目已封装 `/code-review`：

- `.claude/skills/code-review/SKILL.md`
- `.claude/commands/code-review.md`

该命令优先使用 Git MCP 读取 diff 和文件上下文，输出结构化代码审查结果。

## 10. 部署架构

Render 部署配置位于 `render.yaml`。

构建流程：

```text
pip install -r backend/requirements.txt
cd frontend && npm ci && npm run build
```

启动流程：

```text
cd backend && python run_prod.py
```

生产行为：

- Render 注入 `PORT`
- `ENABLE_CORS=false`
- `DATA_DIR=/data`
- SQLite 和上传文件落在 Render Disk
- FastAPI 服务 `frontend/dist`
- 非 API 路由 fallback 到 `index.html`

## 11. 本地开发流程

推荐一键启动：

```powershell
python start.py
```

手动启动：

```powershell
cd backend
python run.py
```

```powershell
cd frontend
npm run dev
```

本地访问：

- 前端：`http://localhost:5173`
- 后端：`http://localhost:8000`
- Swagger：`http://localhost:8000/docs`

## 12. 工程约束与风险

### 12.1 数据库

SQLite 适合当前本地和轻量部署场景，但如果后续做多用户、高并发、长期生产运行，建议迁移到 PostgreSQL。

### 12.2 用户体系

当前架构未体现完整用户登录、鉴权和多用户数据隔离。练习记录、掌握度、AI 对话等数据默认更适合单用户使用。

### 12.3 AI Key 与密钥

项目依赖 `DEEPSEEK_API_KEY` 和 MCP 中的外部服务密钥。密钥不应写入文档、提交到远程仓库或在回答中明文复述。

### 12.4 OCR 依赖

生产依赖保持轻量；复杂 OCR 能力更适合通过 MCP 或本地脚本处理，避免 Render 免费环境构建超时。

### 12.5 页面路由

`Progress` 和 `ReviewPlan` 已作为正式功能挂入主导航，路径分别为 `/progress` 与 `/review-plan`。

### 12.6 当前补齐状态

- 已建立轻量级数据库迁移记录表 `schema_migrations`，可通过 `python scripts/migrate_db.py` 检查。
- 已补充基础后端测试，覆盖题目筛选、判题历史、迁移表、AI Key 缺失提示、健康检查和文本资料上传。
- 题库/资料导入相关脚本已统一输出 JSON 导入报告到 `data/import_reports/`。
- 单用户边界已通过 `APP_MODE`、`APP_USER_LABEL` 和 `/api/health` 暴露给前端。

## 13. 后续演进建议

优先级较高：

1. 继续保持 MCP 密钥通过环境变量注入，并轮换曾经暴露过的外部服务 key。
2. 恢复 Windows Git for Windows/Git Bash/Claude Code 环境，使 Git MCP 和 `/code-review` 可用。
3. 扩展端到端测试，覆盖真实浏览器下的刷题、复习、文档上传和 AI 降级流程。
4. 增加用户体系或继续强化单用户配置边界。
5. 将迁移机制从轻量脚本演进为 Alembic 等正式迁移工具。

中期方向：

1. 文档切片和关键词/语义检索。
2. 知识点与资料段落自动关联。
3. 基于错题和薄弱章节生成阶段复习计划。
4. 编程题和简答题的 AI 辅助评分。
5. 从 SQLite 迁移到 PostgreSQL。

## 14. 总结

本项目已经形成较完整的 804 考研学习闭环。它的核心价值不是单个页面或单个 AI 功能，而是把知识点、题库、错题、AI、文档资料和学习统计连接成一个面向具体考试的学习系统。

当前架构足够支撑本地高效迭代和轻量部署。后续如果要继续产品化，重点应放在数据质量、用户体系、测试覆盖、数据库迁移和知识库检索能力上。
